// preloader - start
  // --------------------------------------------------
 // Wait for the page to load
 // Wait for the page to load
 window.addEventListener('load', function () {
  var preloader = document.getElementById('preloader');
  preloader.style.display = 'none'; // Hide the preloader once the page has fully loaded
});
  
    // preloader - end
    // --------------------------------------------------
  